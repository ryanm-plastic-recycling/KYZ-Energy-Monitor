# Optional branding logo overrides used in Dashboard and Kiosk headers.
# Place this .env file in dashboard/web (same folder as package.json), not inside Public.
# Public assets should use root-based paths, e.g. /pri-logo-vector.png.
# Vite env vars are build-time: restart `npm run dev` after edits and rebuild for production.
VITE_PRI_LOGO_URL=/pri-logo-vector.png
VITE_INNOVATION_LOGO_URL=/Innovation%20Triangle%20-%20avatar.png
